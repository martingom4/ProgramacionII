// JavaScript para manejar los filtros (ejemplo simple)
document.getElementById('author-filter').addEventListener('change', function() {
    // Filtrar productos por autor
});

document.getElementById('editorial-filter').addEventListener('change', function() {
    // Filtrar productos por editorial
});
